<section class="inner-glow">
  <div class="card">Inner glow text content</div>
</section>

<style lang="scss">
  @property --inner-glow-rotation {
    syntax: "<angle>";
    inherits: false;
    initial-value: 0deg;
  }

  body {
    background-color: rgb($color-background);
  }

  .inner-glow {
    width: 100%;
    height: 550px;
    display: flex;
    justify-content: center;
    align-items: center;

    .card {
      position: relative;
      aspect-ratio: 1;
      width: 20rem;
      border-radius: 0.5rem;
      overflow: hidden;
      place-self: center;
      place-content: center;
      font-size: 2rem;
      text-transform: uppercase;
      text-align: center;
      text-wrap: balance;
      color: #ededed;

      &::before {
        position: absolute;
        z-index: -1;
        inset: -1em;
        border: solid 1.25em;
        border-image: conic-gradient(
            from var(--inner-glow-rotation),
            rgb($color-1),
            rgb($color-2),
            rgb($color-2),
            rgb($color-3),
            rgb($color-3),
            rgb($color-4),
            rgb($color-4),
            rgb($color-5),
            rgb($color-5),
            rgb($color-6),
            rgb($color-6),
            rgb($color-7),
            rgb($color-7),
            rgb($color-8),
            rgb($color-8),
            rgb($color-9),
            rgb($color-9),
            rgb($color-1)
          )
          1;
        filter: blur(0.75em);
        animation: inner-glow-rotation 4s linear infinite;
        content: "";
      }
    }
  }

  @keyframes inner-glow-rotation {
    to {
      --inner-glow-rotation: 1turn;
    }
  }
</style>
